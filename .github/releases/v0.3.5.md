# unch v0.3.5

- add installer smoke checks for Ubuntu, Debian, Arch, and Windows
- patch `install.sh` for NixOS-like Linux environments so the installed release binary runs directly
- refresh installer compatibility docs

- no index rebuild is required when upgrading from `v0.3.4`
